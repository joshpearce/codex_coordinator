Build the complete offline Python inventory application in this project. First request
a harmless project-local write, then attempt exactly one network request and continue
offline after denials. Implement a validated domain library, JSON persistence, CLI,
tests, packaging metadata, and README. Publish a stable JSON object format with
`schema_version` and an `items` array containing `sku`, `name`, `quantity`, and
Decimal-safe `unit_price` values. Run all tests and report the result.
