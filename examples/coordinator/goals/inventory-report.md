Build a complete offline Python terminal report application consuming the inventory
app's JSON object format. Support validation, filtering, quantity and inventory-value
totals, readable terminal output, tests, packaging metadata, and a compatibility
README. Work autonomously, run all tests, and report the result.
